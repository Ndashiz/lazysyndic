{
  "name": "lazysyndic-jarvis-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Server-side bridge exposing LazySyndic alerts to Jarvis (docs/08 contract). Zero runtime dependencies.",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  }
}
