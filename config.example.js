/* LazySyndic — configuration Supabase.
   Copiez ce fichier en `config.js` et renseignez vos valeurs.
   (La clé "anon" est PUBLIQUE par conception — protégée par les
   politiques RLS côté base. Elle peut figurer dans le code déployé.)

   Où les trouver : Supabase → Project Settings → API
     - Project URL      → SUPABASE_URL
     - Project API keys → anon public  → SUPABASE_ANON_KEY
*/
window.LAZYSYNDIC_CONFIG = {
  SUPABASE_URL: 'https://VOTRE-PROJET.supabase.co',
  SUPABASE_ANON_KEY: 'VOTRE_CLE_ANON_PUBLIQUE',
};
